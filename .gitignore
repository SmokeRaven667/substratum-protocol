# Commercial rulebook PDF — reference only, not for redistribution.
# See 01-rulebook-digest.md for the extracted mechanics reference instead.
848511358-Substratum-Protocol.pdf

# Editor/OS cruft
.vscode/
Thumbs.db
.DS_Store

# Build tooling
node_modules/

# Compiled compendium packs (LevelDB) — built from packs/_source/ via
# `npm run build:packs`, not committed (avoids binary diffs and LOCK/LOG
# runtime-only files); source JSON in packs/_source/ is the real content.
packs/*
!packs/_source/

# Built release artifact — produced by `npm run build:release`, uploaded
# to GitHub Releases, not committed to the repo.
system.zip

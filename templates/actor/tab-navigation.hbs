<nav class="sheet-tabs tabs" data-group="primary">
  {{#each tabs}}
    <a class="item {{this.cssClass}}" data-action="tab" data-tab="{{this.id}}" data-group="{{this.group}}">
      {{localize this.label}}
    </a>
  {{/each}}
</nav>
